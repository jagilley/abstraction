# rubato — the kinematic program through a body model: ceiling, learned cerebellum, online correction

A child of the `tempo` round with no writeup of its own: its runs are written up in the parent,
[`../README.md`](../README.md#findings) (tempo), Findings §5–§7 (`k1`, `klsmoke1–3`, `kc1`, `kc2`).
**Spec**: [`SPEC.md`](SPEC.md) (the orchestrator's prompt verbatim) · **Files**: [`FILES.md`](FILES.md)
(every decision, gate, smoke, run record and retraction).
