# Files — `one_layer_deeper`

**Up**: [README.md](README.md) (this node) · [../CLAUDE.md](../CLAUDE.md) (experiments)

## Code files

| file | purpose |
|---|---|
| `squaring_mod.py` | The repeated-modular-squaring task, vendored from `tilde-research/one-layer-deeper` (`data/squaring_mod.py` + `data/counting.py`). Verbatim: `TOKEN_IDS`, `DIGIT_OFFSET`, `number_tokens`, `trapdoor_squaring_mod`. Ours: `TaskSpec` (the fixed-`N` DGP knobs, with `describe()` reporting the depth-periodicity margin and reachable-state count), `ModulusFamily` (the **variable-`N` DGP** — a family of moduli under uniform digit width, a minimum depth-repeat margin, and a minimum prime factor, plus the train/held-out split that forms the arity axis), `depth_first_repeat` (the tail+period of `2^T mod λ(N)` — the depth at which the task admits a periodicity shortcut instead of a serial rollout, which any eval range must sit strictly below), `build_trajectories` (full `x_0..x_T` trajectories, cross-checked against the independent trapdoor label path), `tokenize_prompt` / `answer_digits` / `encode_split`. Two documented departures from upstream: it emits intermediate residues (research-only instrumentation, never a training target) and pads answers to fixed width. |
| `shared.py` | Modal infrastructure — image (torch 2.7.0), volume `one-layer-deeper-data`, app `one-layer-deeper`, `NumpyEncoder`. |

## Children

| child | summary |
|---|---|
| [`dress_rehearsal/`](dress_rehearsal/README.md) ([FILES](dress_rehearsal/FILES.md)) | **The organizers' evaluator as a benchmark — harness, port, and the first full-budget Hard measurements.** Runs upstream `4ceff95`'s `benchmark.runner` unmodified on the organizers' data (own Modal H100 for smoke; hosted Easy/Medium/Hard for everything real — free H100 time returning the full seven-rung profile, `status --json`), with a two-arm port (`control` = terminal CE, `closure` = plus `ballistic_depth` §2's label-free cycle + re-entry via `auxiliary`; loop-on-`T`; v3 re-encodes through the real encoder with a progress-gated ramp). 15 hosted runs saved under `results/hosted/`. Findings: the `[12,14,16]` no-reduction floor is **exactly 6/768** = the live board's rank-2–6 tier (multiply learned, reduce not); only 14 twelve-bit moduli exist, so that cell is necessarily dense and rank 1's 127/768 with 0% OOD reads as interpolating it; Hard is modulus-grouped (`test`/`ood_t`/`ood_n_t`); at 3600 s on the hidden data the control arm **memorises 58% and transfers nothing** (0/768 every rung, test loss 11.4) — the rule-acquisition wall at full budget; on fixed `N` with 90% of units seen, control / closure-v2 / closure-v3 read **2 / 4 / 1 of 140** fresh `x` at `T=1` (floor 4), so `ballistic_depth`'s 0.32 **did not reproduce** in the port even with the mechanism verified; v3 sits just above the OOD-`N` floor on two datasets (counts of 2–4, noted not claimed). Also: the modulus population is fixed by arithmetic (**6/21/14/57/42/169/148** at 10–16 bits), `examples_per_setting` is capped near 28k for any 12-bit cell, and **357/768** of `m5`'s `T=64` prompts repeat a lower rung. Best hosted placing: rank 31/109. |
| [`variable_modulus/`](variable_modulus/README.md) ([FILES](variable_modulus/FILES.md)) | **The arity-2 cut — sampling `N` instead of fixing it.** `ballistic_depth`'s headline does not reproduce: the same label-free constraint installs closure just as completely (cos 0.29 → **0.97**) and moves the horizon **10 → 12**, not 13 → 51. The result is a **double dissociation between two independent limits on depth** — the cycle term multiplies closure and leaves the per-restart rate flat (0.894 → 0.895), while state coverage doubles that rate (`p ≈ coverage`, 0.47 → 0.98) and leaves closure and the raw horizon flat. Unaided rollout is drift-limited; re-projected rollout is coverage-limited at `p^⌈T/k⌉`. **No arm learned modular squaring** — held-out `x` and `N` sit at or below the 0.041 no-rule-knowledge floor, with no phase transition over 500k grokking-condition steps. Four of six pre-registered predictions failed, including that re-projection would favour `fold` (identical: 0.613 vs 0.610 — *a static rule is cheap to carry*). An early "more moduli → shorter horizon" claim is **retracted** as a fixed-depth readout artifact. |
| [`rule_acquisition/`](rule_acquisition/README.md) ([FILES](rule_acquisition/FILES.md)) — children: [`exact_atom/`](rule_acquisition/exact_atom/README.md), [`staged_reduce/`](rule_acquisition/staged_reduce/README.md) | **Why the one-step map is memorised — the atom, not its composition.** Its child [`staged_reduce/`](rule_acquisition/staged_reduce/README.md) then **moves the rule axis**, the one nothing in this program had moved, by decomposing the reduce into bounded-quotient stages chained *at test time only* with the remainder re-grounded through the model's own decode — `ballistic_depth` §9's re-projection one level down, exact rather than a manifold projection. Step-matched at 600k: **0.9968** vs a monolithic control's 0.4841 at 3 digits, **0.9899** vs **0.0005** at 4 digits, and **0.9925** on rejection-sampled chains none of whose stage queries was ever trained on, so it is generalisation and not coverage. Held-out `N` goes from floor to **0.9826 / 0.9790** across two seeds (per-modulus min 0.851 over 36 unseen moduli) against the 0.058 below — but only as an **interaction**: a family-matched 8-modulus arm reads 0.9943 on held-out `y` and 0.0042 on held-out `N`, and breadth without staging moves 0.0012 → 0.0025. The compute control is *not* null, and no cell certifies rung `T=1`. Its child re-reads the atom against the **exactness** target Hard gates on — under `ballistic_depth` §9's re-projection at `k=1`, rung `T` needs `eps <~ 9e-4/T`, so the ladder is log-error and rung zero carries the difficulty. It **retracts the multiply result below** (0.920 → **0.068** on a clean split), converts the coverage inference into a measurement (the reduce reads **0.953** on held-out `x` from inside a trained composed model, against `redmod`'s 0.022), and finds the two halves fail at different digit widths — at 4 digits the multiply improves while the *full-range* reduce collapses to 0.0004 and bounded-quotient reads 0.986, localising the reduce's limit to the **quotient range**. A pre-registered memorisation-boundary mechanism failed in both directions; 27.4M parameters gives the node's best transfer (0.909, still improving). At depth 1 with half of every modulus's bases held out, **nine arms sit at or below the no-reduction floor**: base-2 encoding, place-value embeddings, 8 encoder layers, 8 serial operator steps, 4096-wide FFN, 17.75× more moduli, staged supervision on the intermediate product, and a 21M-parameter arm stacking all of them (0.051 vs floor 0.049). But **both halves of the atom generalise alone** — `x -> x^2` at **0.920**, `x^2 -> x^2 mod N` at **0.773–0.996** when the reduce is shown its own input space, against **0.022** on the inputs `x^2` actually supplies. The reduce's space is `N`× larger than the multiply's at equal sample count, so within these scales the atom is memorisation-only because the composition **starves its second stage of coverage**, not because the algorithm is unreachable — which retro-explains all nine nulls. Division degrades gracefully with quotient range (0.996 → 0.982 → 0.773). The surviving barrier is the **rule** axis: held-out `N` at or below floor at 8 moduli, 0.058 (3.8× floor) at 142, both arms undertrained. Also **scopes `rule_structure`'s *unreachable* verdict** — an arm generalising at 0.920 reads at the same permutation null. Single seed; two pre-registered predictions failed. |
| [`ballistic_depth/`](ballistic_depth/README.md) ([FILES](ballistic_depth/FILES.md)) — children: [`rule_structure/`](ballistic_depth/rule_structure/README.md), [`horizon_convergence/`](ballistic_depth/horizon_convergence/README.md) | **What actually extends a learned operator's composition horizon.** At `N=9853` (11.6× the state set) the cycle term at its swept optimum **collapses** (ID 0.001 with closure 0.998) — the cause is the *schedule*, not the weight, and repairing it restores learning but not the advantage — establishing that closure is only interpretable conditional on ID competence. The `N=9853` horizons then proved **unconverged** (2× budget moves T=7 from 0.176 to 0.996 with ID saturated in both runs, while `N=893` is converged under a 2.5× control), so they are lower bounds and no cross-scale horizon comparison is quotable; §1–§9 stand. Its other child finds **no group organisation in the representation beyond a permutation null**, falling *further* toward the null as the state set grows. Terminal-CE-only gives T≈13; one label-free "the state you rolled into must be one your encoder could have produced" constraint gives T≈51 (3.9×) at its swept optimum, and held-out-x 0.001 → 0.32. Mechanism is **manifold closure** (cos 0.19 → 0.99) — and acting on it *at test time*, with no retraining, takes the grounded arm to **1.000 at T=60**. Reproducing *rollability ≠ depth* on a new substrate; the predicted error-amplification mechanism is falsified five ways with the instrument calibrated on-manifold first. A non-recurrent untied stack is perfect at every trained depth and at chance one step past it. Hard quantisation fails to learn twice while achieving genuine per-step contraction — *error correction, achieved and useless* — and the label-reusing half of the consistency term is dose-dependently harmful, inverting the dense/evaluative complementarity the cut was designed around. |
