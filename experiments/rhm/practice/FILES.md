# practice (RHM) — File & Child Index

Summarized in [README.md](README.md); this is the complete listing.

## Children

| Child | What |
|---|---|
| [`crystallize/`](crystallize/README.md) | **Certificate-gated compilation on the sculpting substrate under priced feedback.** Ports the étude's compile op (δ-silence certificate, selection-not-averaging, scoring under the consumption distribution) onto a task with an exact DP oracle and a level-indexed action space. Answers the étude's finding-7 question — state-conditioned commitment beats state-independent by 1.8–3.0×, 6/6 — and finds the certificate vacuous on a frozen plant (commit-at-c1 matches every gated arm at 26× less priced time), with a shadow-compile instrument and a plant precheck that together locate why. Per-file purposes: [crystallize/FILES.md](crystallize/FILES.md) |
| [`ratchet/`](ratchet/README.md) | **Earning a level-indexed vocabulary over a depth-laddered damage schedule.** The compile product is a **macro action** — the true level move's max-sum operator over a table *mined* from the agent's own solved repairs, with `T[l]` defined over `T[l-1]` entries so the ratchet is structural. Earning recovers **68–98%** of the given vocabulary's advantage at 0.85× its priced time and flattens cost-to-depth 4.1×; committing one cycle early is **worse than never committing** because it forecloses the next level's representation; the unit-LP certificate matches its offline prediction at level 2 and correctly refuses at level 3, where a mis-levelled audition understates the unit by 1.75× (the étude's seam law, sign-flipped). The plant stayed inert — a manufactured frontier was tried and failed — so the descent is vocabulary-carried. Per-file purposes: [ratchet/FILES.md](ratchet/FILES.md) |
| [`ear/`](ear/README.md) | **Climbing the evaluation layer.** A 3×2 grader grid (audition context × evaluator) over the ratchet's substrate. Climbing fixes the measurement (consumption-matched in-policy audition calibrated to 0.97 — the seam law's third coordinate) but not the decision (LP starves for arms already holding the prior level's vocabulary); **provisional commitment at the era boundary rescues era 3** and beats `given`, with the live recert firing 0/24 — mining is selection-filtered upstream. Grader cost 5–29% of priced time. Per-file purposes: [ear/FILES.md](ear/FILES.md) |
| [`recital/`](recital/README.md) | **Self-paced eras.** The era clock removed; advancement policy the only variable, commit policy fixed. Three internal pacing signals fail for three distinct measured reasons across three independent worlds, while a fixed bottom-heavy schedule is rank 1 everywhere and beats the DGP's own tables in both independent worlds; corr(first boundary, mean error) −0.94/−0.69/−0.79. Mechanism check: what late buys is not the current level's table (post-saturation increments price at zero) — the seed-stable predictor is the *next* level's table. Stream-position noise bounds single-run fractions at ±0.15. Per-file purposes: [recital/FILES.md](recital/FILES.md) |
| [`tall/`](tall/README.md) | **The depth-6 port attempt (closed).** m=4 measured inadmissible for sculpting; depth-6/m=2 passes every structural gate (ladder gradient 3.31×, exact reader) and the main run is voided by value-head signal starvation at 64 tokens. Two regime-independent findings stand — endogenous pacers cannot traverse a ladder longer than the earnable range (their signals are undefined there), and the mined table cannot churn (a priori) — plus a pricing inversion: at this scale committing costs more beam width than the macros are worth. Discriminator (boundary vs frontier) open; re-run recipe recorded. Per-file purposes: [tall/FILES.md](tall/FILES.md) |
| [`transpose/`](transpose/FILES.md) | **Truth-news: within-run grammar drift.** Installs the news gap every prior round lacked (hard rule-cell resampling, unannounced, level-indexed; `DecayMiner` lets the vocabulary forget). Level-2 drift measured inadmissible (degrades the executor); at level-3 drift the static commit-policy ordering survives, post-commit rise decomposes to world-hardening not invalidation, recert stays near-silent (1/62), and a stale mined table beats the current truth — concentration dominates currency. No README by policy; findings in [`typed_gaps/`](typed_gaps/README.md). Per-file purposes: [transpose/FILES.md](transpose/FILES.md) |
| [`setlist/`](setlist/FILES.md) | **Demand-news: the consumption distribution drifts.** Grammar fixed; OU drift on the root prior and upper-layer rule mixtures, so what is *asked* moves while precision-vs-truth is pinned at 1.000 by gate. Recert wakes under its native currency (9/62 swaps, all coverage-improving; ablation costs Δ0.271 deep, ≈8× noise); the oracle bracket prices concentration (tracking 1.00–1.24 vs frozen 0.35–0.75 e-v-g); decay-forgetting costs coverage while audit-and-reselect works. No README by policy; findings in [`typed_gaps/`](typed_gaps/README.md). Per-file purposes: [setlist/FILES.md](setlist/FILES.md) |
| [`typed_gaps/`](typed_gaps/README.md) | **The crossed pair's joint writeup.** transpose × setlist as one experiment: a double dissociation — truth-drift degrades the dense learner and leaves recert silent; demand-drift leaves the plant flat and fires recert. Chunks store demand-concentration, not truth; skill maintenance is demand-tracking, evaluative not entropic; pre-commit certification stays demoted while post-commit verification is rehabilitated currency-specifically; the conditioning gap becomes a type system (one organ per currency of change). Single seed; ranks are the claim |
| [`reread/`](reread/README.md) | **Re-reading a frozen archive as the vocabulary climbs (rr_s0 + the lm twin).** On sculpting the archive is renewable in competence, not vocabulary (yield decays to zero, no re-arm at commits; metered gains 0.19–0.50 vs dense ≤0.09) — scoped by the instrument's exogenous reader. The child [`reread/lm/`](reread/lm/README.md), with an endogenous reader against exact BP oracles, lands the claim: extraction strictly level-ordered in every arm; a 6.4×-re-read corpus fully renewable at zero token penalty (two instruments); below a corpus-size wall re-reading is capped, not slowed (~10× distinct corpus per half level). Plus: archive depth-mix is a curriculum (deep-only archives mine a worse-than-random L2 that forecloses L3). Single seed. Per-file purposes: [reread/FILES.md](reread/FILES.md), [reread/lm/FILES.md](reread/lm/FILES.md) |
| [`merge/`](merge/README.md) | **The merge op, enumeration-vs-merge, and self-play's holes (mg_s0, sp_s0+sp_s0c).** Rotation and merge are substitutes on task error but not on the next level (enumerated index: operative-cell \|T3\| 12 vs merged 67 vs its own union 68 — the index withholds the content, at every rotation rate); the merge op refuses genuinely distinguishable cells, and paced rotation is what renders them indistinguishable (18→1, 8× less storage). Round 2: a competence-amplified self-play footprint (β=2) is rank 1 by its own grading and second-worst on coverage with 30–70× starved holes and 18% less next-level representability; yoked placement-matched narrowness reproduces only 22% — the damage is the coupling's collapse dynamics, with a cliff onset between β=1.5 and β=2. Single seed. A narrowed independent slice of the same spec: [`fourwall/`](fourwall/README.md). Per-file purposes: [merge/FILES.md](merge/FILES.md) |
| [`teacher_slot/`](teacher_slot/README.md) | **The teacher slot decomposed (tsdB, b1, hr_s0).** `fourwall/lm`'s "op and justification must come from outside" split into rungs: a minimal ABBA paired-trial outer loop reading any one-level-up gauge (pathway readout, or the learner's own d5 next-level yield at a 13× read premium) merges pre-rotation and holds with zero bail-outs, while the same rule reading the experienced loss refuses all five offers and posts nearly the arc's best within-level ledger while ending hollow — the ledger votes the crossing down optimally. A blinded text-trained reasoner then removes the key 6/6 sessions pre-disruption (3/3 on the refuser's own information set; 0/3 dead-token control; 0/2 late join) on measurement-meaning grounds — the slot is a gauge-choice port and post-ratchet text carries its priors. Sideline: a backprop handle for earned macros moves the inert plant *negatively*, dose-ordered. Single writeup for the whole node; per-file purposes: [teacher_slot/FILES.md](teacher_slot/FILES.md) |
| [`native/`](native/README.md) | **The port back (np_s0, sp_s0, nf_s0).** The earned vocabulary consolidated into the learner itself: a proposal head routes to chunks (beats enumeration at matched price — freed groundings buy width; the primitives-only control shows the chunk is the object, 2.2–2.5×), a span head runs the corridor in one pass without `handle/`'s interference (the §3½ identity/corridor split confirmed as the variable), and after consolidation deleting the table costs ~zero at the current level while the next level's representation still requires it — the address book is for growth, not performance. Can't-decompose signature present (~0.12); native routing raises L3 minability; the poison twin inverts (routing quarantines a bad address, 6× fewer bogus calls). Single writeup for the node; children carry `FILES.md` records: [native/FILES.md](native/FILES.md) |
| [`fourwall/`](fourwall/README.md) | **Index-news: the spurious key and its whittling (fw_s0–fw_s3).** A free surface token perfectly correlated with the true level-2 latent under phase-1 demand, then rotated away unannounced. The scaffold delivers oracle-index value for free and the debt is +0.19–0.41 per rotation; the whittling decomposes into merge (deletes, recovers only the no-index baseline), re-key (functional addressing — "same unit serves them" — closes a third of the terminal gap and extinguishes the rotation response), and retire (recovers the 16→10% maintenance rent; mothball-vs-delete priced by demand recurrence via the c121 revival). A good scaffold raises the bar for its own replacement; coverage-graded gates lock it in forever (seam law, evaluator coordinate, on the index); the earned quotient is capped by the unit vocabulary; no spontaneous binding. Machinery, Gate 0 and calibration: [fourwall/FILES.md](fourwall/FILES.md) |

| [`spiral/`](spiral/README.md) | **The live re-earning spiral on the depth-6 substrate (pa0, sp_s0).** Earn level k live, consolidate into planner+executor, earn level k+1 natively over the routed policy — `tall/`'s recipe satisfied, its descent gate passed and its pricing inversion dissolved by routing (enum's commit penalty permanent, routing's a 2-cycle transient). The crank holds on the certificate's clock (net cycles-to-cert ~constant per turn), decays on the coverage clock (L3 committed recall 0.05–0.09 in every earning arm), holds on the value clock inside the earnable range (earned ≥ given-native, ~half enum's priced time) — the grader is **satisfiable by concentration**, and the diet and corridor walls did not bite. Address book survives on a live-earned vocabulary; both-ports-vs-routing consumption gap now a two-node pattern. Per-file purposes and per-decision measured reasons: [spiral/FILES.md](spiral/FILES.md) |
| [`census/`](census/README.md) | **Can an endogenous gauge buy the coverage the certificate can't see (cs_s0 + the forensics pass)?** Coverage is buyable (gate: +0.072 L2 recall; extension: 2.5× L3) and does not close the deep-era gap; gate-later is dead (gate ≡ yoked bit-for-bit — criterion epiphenomenal, hold free); the forensics refute the artifact suspects and find the mechanism: π vets levels, not entries, so precision-degrading additions de-fund the level while precision-preserving ones help. Carries the unit's super-writeup ([census/README.md](census/README.md)); per-file record: [census/FILES.md](census/FILES.md) |
| [`assay/`](assay/README.md) | **Which ingredient of the complete vocabulary is the currency (as_s0, as_s1)?** Oracle surgery crossing amount × truth × arrival, plus a per-execution entry-identity instrument and stream-displaced twins. Arrival dominates (era-4 residual 3.24× the measured stream floor; deep-era ranks invariant under displacement); the executor DP filters junk at execution (6.2%-true table → 71–76% true executions); pure coverage buys a certified minority (3.56× floor); the trust signature (π L3 mass/argmax) separates from-birth from late arrival robustly. Written up jointly in [census/README.md](census/README.md); per-file record: [assay/FILES.md](assay/FILES.md) |
| [`conductor/`](conductor/README.md) | **A1 — the composition: the outer loop takes the certificate's seat (cd_ef, cd_s0).** `teacher_slot`'s rule ported to absorbing actions (a thermostat on a paired-interval slope, dead zones measured by null-ABBA at full config, offline vs in-tag ledger floors agreeing to 0.09%) drives commit/hold and era advance over the assay stack. The free at_support reader paces the crank as well as the schedule inside the earnable range (era-3 Δ 0.000) and better beyond it (era-5 +0.251, 2.9× the earning floor) via bigger same-precision tables; the within-level reader re-derives the refusal and *starves its own next level's observation stream*; the raw one-level-up NLL never arms (no second condition to cancel plant drift) and its excess form is a one-shot license; both yoked clocks are bit-identical to their gauge arms — census finding 4 on the full action set. Per-file purposes and the rule stated plainly: [conductor/FILES.md](conductor/FILES.md) |
| [`maestro/`](maestro/README.md) | **A2 — learn the rule (ma_s0, ma_s1).** A1's thermostat replaced by a level-state-indexed learned gauge mixture (contains the thermostat exactly as one point; fitted offline on the logged corpus — absorbing actions make within-run exploration impossible), with a within-level-rewarded twin of the identical class as the negative control. The reward's type makes the judge: near-orthogonal mixtures from identical inputs; the within-level twin commits pre-certificate and never takes L3 while reading the yield gauge at 2+ floor units; the yield-rewarded rule matches-or-beats the thermostat at ~8% less priced time by committing earlier with thinner tables. Stream twins demote the era-5 learned-vs-thermostat ordering (sign flips), hold era 4, and confirm each gauge arm's era-5 win over the schedule at 2.3–3.8× floor on both draws. Per-file purposes and the learned class stated plainly: [maestro/FILES.md](maestro/FILES.md) |
| [`crescendo/`](crescendo/README.md) | **A3 — the signature (cr3_s0, cr3_s1).** L4 opened to the crank on the depth-6 world; Phase 0 sized the whole round offline (the r² wall, the era-3 commit window, the L5 gauge's degeneracy) and its predictions landed (commit cycle and book size, both draws). The loop-paced learner commits L4 on its own signals; against a lifetime-identical clock-yoked ceiling differing in exactly one bit, the value clock holds past the previously-certified range on both stream draws (era 4 +0.388/+0.427, 12.9× the in-node displacement floor; era 5 sign-stable), with π's L4 trust forming monotonically post-commit and 0.000 in every forbidden arm. Extension doubles the books (the r²-wall lever → F3 on the critical path). Per-file purposes, Phase 0, and the one-bit control stated plainly: [crescendo/FILES.md](crescendo/FILES.md) |
| `critic/`[^private] | **A learned grade of record, calibrated against the exact one (cals_s0, cr_s0, calc_s0).** De-risking for the image port: every practice node's grade is `crystallize/units.grade`, consumed four ways on the sculpting substrate, and the image substrate has none of it. A root-conditioned NTP reader trained on held-out clean derivations, thresholded at an oracle-free quantile of exemplar typicality, is swapped in at every AGENT-consumed point (value target, mining gate, audition) with the exact grade logged only, and [`style_practice_substrate`](../../../ideas/style_practice_substrate.md) §5's four instruments are scored exactly for the first time. Forks `ratchet/` with a bit-for-bit gate on the donor's five arms (G-F). No README by policy while the numbers are under discussion; design record: critic/SPEC.md[^private]; per-file purposes: critic/FILES.md[^private] |
| `tuning/`[^private] | **Does a two-basis discrepancy type an OOD event, does the type pick the op, and does a forward self-model grade the op? (spec, unbuilt.)** On `fourwall/lm`'s world with span-local irreducible noise bursts interleaved with rotations (and `transpose`-style drift as an extension), two factors crossed: **T**, a typing gauge at the event instant holding the key reader and the parse reader together (`D_pair`, or `teacher_slot`'s one-model counterfactual `D_self`) — rotation → merge, burst → skip (the aleatoric null as an action), drift → track — against the surprisal gauge forced to run one policy for every spike; and **M**, a depth FM over the keyed reader whose benchmark-subtracted residual reads whether the *computation* moved over the following window and whether that movement was reducible (the performance-error object; `OOD_ROBUSTNESS`'s input-invariance as the premise), grading and reverting the op — against the ABBA trial it is meant to replace, and standing alone in `sur_fm`. Graded per event in nats against the correct op plus the arc's two standing currencies. Gate 0 is an uncharged shadow panel for both factors; Gate 3 asks whether one trunk can carry the second basis. Design record: tuning/SPEC.md[^private] |

## Code files

No code at this level; every script lives in a child. See
[crystallize/FILES.md](crystallize/FILES.md) for `units.py`, `crystallize.py`,
`analyze_crystallize.py` and `launch_detached.py`;
[ratchet/FILES.md](ratchet/FILES.md) for `macros.py`, `ratchet.py`, `analyze_ratchet.py`
and its `launch_detached.py`; [ear/FILES.md](ear/FILES.md) for `grader.py`, `ear.py` and
`analyze_ear.py`; [recital/FILES.md](recital/FILES.md) for `recital.py` and
`analyze_recital.py`; [tall/FILES.md](tall/FILES.md) for `tall.py` and `analyze_tall.py`;
[transpose/FILES.md](transpose/FILES.md) for `drift.py`, `transpose.py` and
`analyze_transpose.py`; [setlist/FILES.md](setlist/FILES.md) for `demand.py`, `setlist.py`
and `analyze_setlist.py`; [fourwall/FILES.md](fourwall/FILES.md) for `wall.py`, `rekey.py`,
`fourwall.py`, `analyze_fourwall.py` and its `launch_detached.py`; [native/FILES.md](native/FILES.md) for the `prop/`, `span/` and `full/` port forks;
[spiral/FILES.md](spiral/FILES.md) for `spiral.py` and `analyze_spiral.py`;
[census/FILES.md](census/FILES.md) for `census.py`, `phase0_replay.py`, `forensics.py` and
`analyze_census.py`; and [assay/FILES.md](assay/FILES.md) for `assay.py` and
`analyze_assay.py`. Each later child imports its parents' substrate rather than copying it.

[^private]: Not mirrored: this link points to a document in the private lab repo (the roadmap, the queue, an unrun spec, reading notes, or a conversation). See the top-level README for what is held back and why.
