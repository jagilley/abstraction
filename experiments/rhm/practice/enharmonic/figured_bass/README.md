# figured_bass — commit the key, not the content

A child of `enharmonic` with no writeup of its own: its runs (`fb_s0`, `fb_s1`) are written up in
the parent, [`../README.md`](../README.md) (enharmonic), section "figured_bass — commit the key, not
the content".
**Spec**: [`SPEC.md`](SPEC.md) · **Files**: [`FILES.md`](FILES.md) · **Facts**: [`sizing/SIZING.md`](sizing/SIZING.md).
