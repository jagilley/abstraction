# en_smoke was the floor-measuring run; its per-arm results.json is ~200-350 MB (pretty-printed,
# log["entry"] ~0.5-1 MB/cycle) and lives on the Modal volume. The floors it produced are checked in
# as floors_en_smoke*.json. Main-run mirrors are checked in only in the compact form.
figures/en_smoke/
