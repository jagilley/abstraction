# tiles_twin — the aligned/misaligned twin, the tail re-analysis, the adjacency-support grader

A child of `plant` with no writeup of its own: its runs are written up in the parent,
[`../README.md`](../README.md#findings) (plant), Findings "Round 2 — the tiles twin" and
"Round 3 — the grader against known validity".
**Design record**: [`SPEC.md`](SPEC.md) · **Files**: [`FILES.md`](FILES.md).
